```diff
-- Although postfix number indicates implicit order, left/right notation explicitises it
- public static bool operator !=(Fraction fraction1, Fraction fraction2)
+ public static bool operator !=(Fraction leftFraction, Fraction rightFraction)

-- min/max if not reserved may still overlap with function names
- const getNumberFromRange = (min: number, max: number): number => {
+ const getNumberFromRange = (lowerLimit: number, upperLimit: number): number => {


-- rpn might be a list of tokens, might be just infix string notation
- foreach (var letter in rpn)
+ foreach (var letter in rpnString)


-- either this or frostbite/sunburn
- int temperature;
+ int temperature_celcium;

-- List is a collection in abstract sense, in programming array-like data structure
-- which is not what {} is Python/Javascript is
- usersList = {...}
+ userDict = {...}

-- Later on in the loop it becomes unclear what `v` is 
- auto v:
+ auto character:


-- Indexing lists[0] and list1 are exclicit and implicit ordering respectively
- def t2_13(l1: LinkedList[T], l2: LinkedList[T]) -> LinkedList[T]:
+ def t2_13(*lists: LinkedList[T]) -> LinkedList[T]:


-- What was thinking, if any?
- def t2_11(l: LinkedList2[T]) -> bool:
+ def has_cycle(l: LinkedList2[T]) -> bool:


-- If only not americans with a dog-sized hole in the ground...
- void handle_key_press(char character, int *speed, int *flag) {
+ void handle_key_press(char character, int *speed_ms, int *flag) {


-- Not here for English exam
- function findTheDifference(s, t) {
+ function getDifference(s, t) {


-- Or Haskell's cons
- def add_in_tail(self, item: Node[T]) -> None:
+ def append(self, value: T) -> Node[T]:

-- `snoc` for reverse
- def add_in_head(self, item: Node[T]) -> None:
+ def prepend(self, value: T) -> Node[T]:
```

