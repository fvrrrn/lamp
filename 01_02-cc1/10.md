```diff
# Boolean variable instead of raw condition in DynArray.delete()
- if self.count < 0.5 * self.capacity:
+ should_shrink = self.count < self.capacity // 2
+ if should_shrink:


# Float division then cast makes intent ambiguous
- new_capacity = max(int(self.capacity / 1.5), 16)
+ new_capacity = max(self.capacity * 2 // 3, 16)


# `asc` you have to beforehand what it means
# although are only nouns and adjective better to add `is`
- def __init__(self, asc):
-     self.__ascending = asc
+ def __init__(self, is_ascending: bool):
+     self.__is_ascending = is_ascending


# Boolean prefix missing on property, reads like a noun, not a predicate
- @property
- def is_asc(self) -> bool:
+ @property
+ def is_ascending(self) -> bool:


# Mixed-type arithmetic: ord() returns int, power grows unboundedly as int
# Guard with modulo at each step (already done in t09.py, missing in t11.py hash1/hash2).
- hash_value += ord(c) * power
+ hash_value = (hash_value + ord(c) * power) % HASH_MODULO


# Bool used as int via implicit conversion, legal in Python but hides type intent
- self.__size += self.slots[index] != key
+ slot_is_new = self.slots[index] != key
+ self.__size += int(slot_is_new)


# Magic float 0.5 compared against an integer counter:
# mixing types and hiding what "half capacity" means
- if self.count < 0.5 * self.capacity:
+ half_capacity = self.capacity // 2
+ if self.count < half_capacity:


# Integer floor-division written as float division followed by int():
# semantics are identical but the float path is misleading.
- steps = self.size // self.step + 1
  # (already correct; counterexample from seek_slot before refactor)
- for _ in range(self.size / self.step + 1):   # float range TypeError at runtime
+ for _ in range(self.size // self.step + 1):


# Avoid float equality: load-factor threshold written as exact float comparison
# is fragile on platforms where 0.5 is not representable exactly.
# (hypothetical guard that could appear in a resize check)
- if load_factor == 0.75:
+ if load_factor >= 0.75:


# compare() returns a magic int (-1, 0, 1) compared against another magic int.
# Extract the sentinel values to named constants so the reader knows what -1 means
- if self.compare(val, node.value) == -1:
+ LESS_THAN = -1
+ if self.compare(val, node.value) == LESS_THAN:


# Bare string literal used as a sentinel / type tag = magic string
- if status == "done":
+ TASK_STATUS_DONE = "done"
+ if status == TASK_STATUS_DONE:


# Make the loop exit reason is self-documenting
- while not isinstance(node, Dummy):
+ is_real_node = not isinstance(node, Dummy)
+ while is_real_node:
+     ...
+     is_real_node = not isinstance(node, Dummy)
```
