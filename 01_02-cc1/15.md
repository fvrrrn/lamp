## 15.1. Comments added where appropriate

1. `t03.py` non-obvious capacity choice in DynArray

```diff
 def resize(self, new_capacity):
     new_array = self.make_array(new_capacity)
     for i in range(self.count):
         new_array[i] = self.array[i]
     self.array = new_array
     self.capacity = new_capacity

 def append(self, itm: T):
+    # doubling keeps amortized O(1) per append
     if self.count == self.capacity:
         self.resize(2 * self.capacity)
```

2. `t03.py` shrink in DynArray.delete

```diff
 def delete(self, i: int):
     ...
     self.count -= 1
+    # shrink at 50% fill but only to 2/3 capacity to avoid thrash on repeated add/delete
     if self.count < 0.5 * self.capacity:
         new_capacity = max(int(self.capacity / 1.5), 16)
         self.resize(new_capacity)
```


3. `t04.py` / `t05.py`  why sentinel nodes instead of None

```diff
+# head.next and tail.prev are always valid Node or Sentinel,
 class _Sentinel[T]:
     prev: "_Sentinel[T] | Node[T]"
     next: "_Sentinel[T] | Node[T]"
```

4. `t01.py`  collect-then-mutate order in LinkedList.delete

```diff
 def delete(self, val: T, all: bool = False) -> None:
+    # find_all is called first so iteration is not invalidated by node removal
     for node in self.find_all(val):
```

5. `t07.py`  direction tuple trick in OrderedList.add

```diff
 def add(self, value):
     node = self.head
+    # (0, 1) means "stop before a node that is equal or greater";
+    # (0, -1) means "stop before a node that is equal or smaller"
     direction = (0, 1) if self.__ascending else (0, -1)
```

6. `t06.py`  bounded loop in Deque.__iter__

```diff
 def __iter__(self):
     node = self._head.next
+    # avoid infinite loop
     for _ in range(self._size):
```

7. `t07.py`  early-exit optimization in OrderedList2.find

```diff
 def find(self, val: T) -> Optional[Node2[T]]:
     node = self.__head.next
     if not isinstance(self.__head.next, Dummy):
+        # skip full scan when val is out of the sorted range
         if self.__ascending and self.compare(val, node.value) == -1:
             return None
         elif not self.__ascending and self.compare(val, node.value) == 1:
             return None
```

## 15.2.  Unnecessary comments removed, code made self-documenting

1. `t04.py`  Stack.pop docstring restates what the signature already says

```diff
 def pop(self) -> T | None:
-    """
-    Removes and returns the top item from the stack.
-    O(1) time complexity and O(1) space complexity.
-
-    If the stack is empty, returns None.
-
-    Returns:
-        T | None: The value at the top of the stack if present,
-        otherwise None if the stack is empty.
-    """
     match self._head.next:
```

2. `t04.py` Stack.push docstring repeats the signature

```diff
 def push(self, value: T) -> None:
-    """
-    Adds a new value to the top of the stack.
-    O(1) time complexity and O(1) space complexity.
-
-    This method creates a new node and places it at the front of the stack.
-
-    Args:
-        value (T): The value to be added to the stack.
-
-    Returns:
-        None: This method does not return a value.
-    """
     node = Node(value)
```

3. `t05.py` dead commented-out line in Queue.enqueue

```diff
 def enqueue(self, value: T) -> None:
     ...
     self._size += 1
-    # return node
```


4. `t05.py` Queue.dequeue docstring restates the signature

```diff
 def dequeue(self) -> T | None:
-    """Remove and return the first element from the queue
-    O(1) time complexity and O(1) space complexity.
-
-    Returns:
-        T | None: The value of the dequeued node, or None if the queue is empty.
-    """
     match self._head.next:
```

5. `t07.py` inline comments narrate obvious comparison logic

```diff
 if not isinstance(self.__head.next, Dummy):
-    # node=Node(2) and val=1 return None because 2 is the smallest number
     if self.__ascending and self.compare(val, node.value) == -1:
         return None
-    # node=Node(5) and val=6 return None because 6 is the largest number
     elif not self.__ascending and self.compare(val, node.value) == 1:
         return None
```
