1. `t07.py` mumbling: "just to be safe" names the feeling, not the reason

```diff
-    # just to be safe there won't be type errors because of Literal[-1, 0, 1]
     def compare(self, v1: T, v2: T) -> int:
```

2. `t07.py` noise: inline examples belong in tests, not next to the code

```diff
     if not isinstance(self.__head.next, Dummy):
-        # node=Node(2) and val=1 return None because 2 is the smallest number
         if self.__ascending and self.compare(val, node.value) == -1:
             return None
-        # node=Node(5) and val=6 return None because 6 is the largest number
         elif not self.__ascending and self.compare(val, node.value) == 1:
```

3. `t07.py` TLDR

```diff
 def find(self, val: T) -> Optional[Node2[T]]:
-    # greedy-iest way to do that
-    # but linked list optimizations barely improve speed
-    # it is best to have array-based implementation with binary search from the next task
+    # TODO: replace with binary search once array-based version is added
     node = self.__head.next
```

4. `t05.py` 

```diff
     self._size += 1
-    # return node
```

5. `t05.py` inaccurate: cycles cannot form here, the real reason is corrupted pointers

```diff
-    # prevent infinite loop if cycles are present
+    # size-bounded stops early if next pointer is corrupted
     for _ in range(self._size):
```

6. `t05.py` "My eyes are here", wtf do I have to lool up anywhere

```diff
-    # unreachable because of check above
     raise IndexError("Index out of range")
```

7. `t04.py` We get it, awesome

```diff
-    # can do protocol that enforces T to have __str__ method
     return ", ".join(str(value) for value in self)
```

8. `t03.py` inaccurate: typo changes the meaning

```diff
-    # tests mail fail if constructor takes parameters so magic number default are used
+    # tests may fail if constructor takes parameters so magic number defaults are used
```

9. `t04.py` types and LSP hover exist

```diff
 def peek(self) -> T | None:
-    """
-    Returns the value at the top of the stack without removing it.
-    O(1) time complexity and O(1) space complexity.
-
-    If the stack is empty, returns None.
-
-    Returns:
-        T | None: The value at the top of the stack if present,
-        otherwise None if the stack is empty.
-    """
     match self._head.next:
```

10. What was I thinking?

```diff
-items = []
-for source in sources:
-    items.append(source.load())
-# populates items from all sources
+# populates items from all sources
+items = []
+for source in sources:
+    items.append(source.load())
```

11. AI comments

```diff
-count = 0  # initializing count to 0
+count = 0
```

12. AI chapter markers

```diff
-# ============ MAIN LOGIC ============
 def process(self):
```

13. AI buzzwords

```diff
-# leverages sentinel pattern for robust edge-case handling
 class _Sentinel[T]:
```

14. Have seen a few, not by me though

```diff
 for item in collection:
     for sub in item.children:
         process(sub)
-    # end inner loop
-# end outer loop
```

15. There are genders, such as comment exactly one line before code, comment on the same line but format so each line uses same spacing,
Mental illnesses: putting comment one the same line with different linings

```diff
 def enqueue(self, value: T) -> None:
-    # server processes queue every 500ms via background thread in main.py
     node = Node(value)
```

