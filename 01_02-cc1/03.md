## 7.1. Booleans

```diff
- flag
+ is_facing_right  

- done 
+ is_done

- visible
+ is_visible  

- error
+ has_validation_error  

- active 
+ is_session_active  
```

## 7.2. Common boolean variable names
```
typedef struct {
  int has_value;
  void *value;
} Maybe;

if (is_digit = my_isdigit(c))


int is_empty(Node *root) { return root->next == root; }

int should_stop;
```

## 7.3. I for Index

```diff
- for k in combinations(range(n), r):
-     sub = tuple(iterable1[i] for i in k)
+ for combination in combinations(range(n), r):
+     sub = tuple(iterable1[i] for character in combination)

- if all(sub_counts[s] == required_counts[s] for s in required):
-     yield ''.join(s)
+ if all(sub_counts[character] == required_counts[character] for character in required):
+     yield ''.join(sub)

- for i, w in enumerate(weights):
+ for i, weight in enumerate(weights):
```




## 7.4. Antonymic booleans

```
is_enabled == not is_disabled

is_nothing == not is_just

is_left == not is_right  
```

## 7.5. Only temporal is constant

```diff
- int tmp = *x;
+ int current_x = *x;

- tmp = set(iterable1).difference(set(iterable2))
+ iterable1_difference_iterable2 = set(iterable1).difference(set(iterable2))


- tmp = production.split('→')
- lhs = tmp[0]
- rhs = tmp[1]
+ [lhs, rhs] = production.split('→')


- BSTreeNode<string> tmp = traverseQueue.Dequeue();
+ BSTreeNode<string> node = traverseQueue.Dequeue();
```
