```diff
# Public field leaks moveSpeed to any other script in the scene
- public float moveSpeed = 5f;
+ [SerializeField] private float moveSpeed = 5f;


# Public field lets other scripts overwrite jumpForce without going through a method
- public float jumpForce = 10f;
+ [SerializeField] private float jumpForce = 10f;


# Public Rigidbody2D lets any script yank the reference out and call physics methods on it
- public Rigidbody2D rb;
+ [SerializeField] private Rigidbody2D rb;


# Loop counter declared before the loop stays alive in the method after the loop ends
- int i;
- for (i = 0; i < coins.Length; i++)
-     coins[i].Collect();
+ for (int i = 0; i < coins.Length; i++)
+     coins[i].Collect();


# newVelocity is assigned and user on the very next line
- Vector2 newVelocity = new Vector2(horizontal * moveSpeed, rb.velocity.y);
- rb.velocity = newVelocity;
+ rb.velocity = new Vector2(horizontal * moveSpeed, rb.velocity.y);


# hit lives across the whole method but is only meaningful inside the if block below it
- RaycastHit2D hit = Physics2D.Raycast(transform.position, Vector2.down, groundDist, groundLayer);
- ...
- if (hit.collider != null)
-     isGrounded = true;
+ if (Physics2D.Raycast(transform.position, Vector2.down, groundDist, groundLayer).collider != null)
+     isGrounded = true;


# currentHealth public gives any script permission to set it to arbitrary values
- public int currentHealth = 100;
+ [SerializeField] private int maxHealth = 100;
+ private int currentHealth;
+ void Awake() { currentHealth = maxHealth; }


# score public makes the write path available to every script in the project
- public int score = 0;
+ private int score = 0;
+ public int Score => score;


# wallHit declared at class scope but used only inside FixedUpdate
- private RaycastHit2D wallHit;
- void FixedUpdate() {
-     wallHit = Physics2D.Raycast(transform.position, Vector2.right * facing, wallDist, wallLayer);
-     if (wallHit.collider != null) FlipDirection();
- }
+ void FixedUpdate() {
+     var wallHit = Physics2D.Raycast(transform.position, Vector2.right * facing, wallDist, wallLayer);
+     if (wallHit.collider != null) FlipDirection();
+ }


# nearbyEnemies list created at class scope but rebuilt every frame inside Update
- private List<Collider2D> nearbyEnemies = new();
- void Update() {
-     nearbyEnemies.Clear();
-     foreach (var col in Physics2D.OverlapCircleAll(transform.position, detectionRadius))
-         if (col.CompareTag("Enemy")) nearbyEnemies.Add(col);
- }
+ void Update() {
+     var nearbyEnemies = new List<Collider2D>();
+     foreach (var col in Physics2D.OverlapCircleAll(transform.position, detectionRadius))
+         if (col.CompareTag("Enemy")) nearbyEnemies.Add(col);
+ }


# ground check is an inline expression repeated in Jump(), Attack(), and Dash()
- bool grounded = Physics2D.OverlapCircle(groundCheck.position, checkRadius, groundLayer);
- if (grounded && Input.GetButtonDown("Jump"))
+ private bool IsGrounded =>
+     Physics2D.OverlapCircle(groundCheck.position, checkRadius, groundLayer);
+ if (IsGrounded && Input.GetButtonDown("Jump"))


# jump input, physics impulse, animation trigger, and sound are all in one if block
- if (Input.GetButtonDown("Jump") && IsGrounded) {
-     rb.AddForce(Vector2.up * jumpForce, ForceMode2D.Impulse);
-     animator.SetTrigger("Jump");
-     jumpSound.Play();
- }
+ private void PerformJump() {
+     rb.AddForce(Vector2.up * jumpForce, ForceMode2D.Impulse);
+     animator.SetTrigger("Jump");
+     jumpSound.Play();
+ }
+ if (Input.GetButtonDown("Jump") && IsGrounded) PerformJump();


# damage application and death check are inline inside OnTriggerEnter2D
- currentHealth -= damage;
- if (currentHealth <= 0) {
-     animator.SetTrigger("Die");
-     Destroy(gameObject, 0.5f);
- }
+ private void TakeDamage(int damage) {
+     currentHealth -= damage;
+     if (currentHealth <= 0) Die();
+ }
+ private void Die() {
+     animator.SetTrigger("Die");
+     Destroy(gameObject, 0.5f);
+ }


# enemy reference kept at class scope so it outlives the trigger callback that used it
- private Enemy currentEnemy;
- void OnTriggerEnter2D(Collider2D col) {
-     currentEnemy = col.GetComponent<Enemy>();
-     currentEnemy?.TakeDamage(attackDamage);
- }
+ void OnTriggerEnter2D(Collider2D col) {
+     var hitEnemy = col.GetComponent<Enemy>();
+     hitEnemy?.TakeDamage(attackDamage);
+ }


# patrolTarget reassigned each frame but keeps scope open across the whole MonoBehaviour
- private Transform patrolTarget;
- void Update() {
-     patrolTarget = waypoints[currentWaypointIndex];
-     transform.position = Vector2.MoveTowards(transform.position, patrolTarget.position, moveSpeed * Time.deltaTime);
- }
+ void Update() {
+     var patrolTarget = waypoints[currentWaypointIndex];
+     transform.position = Vector2.MoveTowards(transform.position, patrolTarget.position, moveSpeed * Time.deltaTime);
+ }
```
