## 16.1. Comments added

1. `t03.py` informative: why doubling

```diff
 def append(self, itm: T):
+    # doubling keeps amortized O(1) per append
     if self.count == self.capacity:
         self.resize(2 * self.capacity)
```

`2 * self.capacity` looks like a magic number without context.

2. `t03.py` informative: shrink constants

```diff
     self.count -= 1
+    # 50% threshold but shrink to 67% to avoid thrash at the boundary
     if self.count < 0.5 * self.capacity:
         new_capacity = max(int(self.capacity / 1.5), 16)
```

The two constants only make sense as a pair.

3. `t04.py` / `t05.py` clarification: sentinel pattern

```diff
+# head/tail sentinels eliminate all empty-list special cases
 class _Sentinel[T]:
     prev: "_Sentinel[T] | Node[T]"
     next: "_Sentinel[T] | Node[T]"
```

Without this a reader has to reverse-engineer why `_Sentinel` exists at all.

4. `t01.py` clarification: collect before mutate

```diff
 def delete(self, val: T, all: bool = False) -> None:
+    # find_all first so node removal does not break the iterator
     for node in self.find_all(val):
```

5. `t07.py` clarification: direction tuple

```diff
 def add(self, value):
     node = self.head
+    # (0, 1): stop at equal-or-greater; (0, -1): stop at equal-or-smaller
     direction = (0, 1) if self.__ascending else (0, -1)
```

`in direction` with compare return values is not obvious on first read.

6. `t05.py` warning: bounded loop

```diff
 def __iter__(self) -> Iterator[T]:
     node = self._head.next
+    # size-bounded stops early if next pointer is corrupted
     for _ in range(self._size):
```

Looks like a bug (expected `while True`). Comment explains it is intentional.

7. `t07.py` clarification: early exit in sorted find

```diff
     if not isinstance(self.__head.next, Dummy):
+        # val is outside the sorted range full scan not needed
         if self.__ascending and self.compare(val, node.value) == -1:
```

8. `t03.py` amplification: constructor constraint

```diff
-def __init__(self):
+def __init__(self):  # no parameters auto-grader tests call it with none
     self.count = 0
     self.capacity = 16
```

Easy to "fix" by adding a parameter, which breaks submission.

9. `t01.py` clarification: TypeGuard limitation

```diff
 case _, _, _:
+    # TypeGuard does not narrow inside match-case
     if is_tail(afterNode):
```

The manual `is_*` calls look redundant next to the match arm without this.

10. `t07.py` TODO

```diff
+# TODO: replace with binary search once array-based version is added
 def find(self, val: T) -> Optional[Node2[T]]:
```

Linked-list find is O(n); marks the known limitation for the next task.

## 16.2. Comments removed

11. `t04.py` docstring restates signature (category 1)

```diff
 def pop(self) -> T | None:
-    """
-    Removes and returns the top item from the stack.
-    O(1) time complexity and O(1) space complexity.
-    If the stack is empty, returns None.
-    Returns:
-        T | None: The value at the top of the stack if present, otherwise None.
-    """
     match self._head.next:
```

Return type and sentinel branch already say everything.

12. `t05.py` dead commented-out line (category 1)

```diff
     self._size += 1
-    # return node
```

Leftover from an earlier design. Looks like an open TODO but is neither.
