1. Queue instead of array for sequential command processing

```diff
-Command[] pendingCommands = new Command[MAX_COMMANDS];
-int head = 0, tail = 0;
-pendingCommands[tail++] = newCommand;
-Command next = pendingCommands[head++];
+Queue<Command> pendingCommands = new Queue<Command>();
+pendingCommands.Enqueue(newCommand);
+Command next = pendingCommands.Dequeue();
```

2. Stack instead of array for undo history

```diff
-Action[] history = new Action[MAX_HISTORY];
-int historyTop = -1;
-history[++historyTop] = action;
-Action undone = history[historyTop--];
+Stack<Action> history = new Stack<Action>();
+history.Push(action);
+Action undone = history.Pop();
```

3. HashSet instead of array for unique collected items

```diff
-string[] collectedKeys = new string[MAX_KEYS];
-int collectedCount = 0;
-bool alreadyHas = false;
-for (int index = 0; index < collectedCount; index++)
-    if (collectedKeys[index] == keyId) { alreadyHas = true; break; }
-if (!alreadyHas) collectedKeys[collectedCount++] = keyId;
+HashSet<string> collectedKeys = new HashSet<string>();
+collectedKeys.Add(keyId);
```

4. Sequential iteration

```diff
-for (int index = 0; index < enemies.Length; index++)
-    enemies[index].TakeDamage(splashDamage);
+foreach (Enemy enemy in enemies)
+    enemy.TakeDamage(splashDamage);
```

5. LinkedList instead of array

```diff
-Enemy[] activeEnemies = new Enemy[MAX_ENEMIES];
-int activeCount = 0;
-for (int index = k; index < activeCount - 1; index++)
-    activeEnemies[index] = activeEnemies[index + 1];
-activeCount--;
+LinkedList<Enemy> activeEnemies = new LinkedList<Enemy>();
+activeEnemies.Remove(targetEnemy);
```
