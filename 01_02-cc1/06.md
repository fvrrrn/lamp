```diff
# Every editor supports fuzzy completion so typing <s><t><l> will return the name regardless abbreviation
- string GetLabel(STLink *pLnk)
+ string GetLabel(SuffixTreeLink *link)

# Also maybe remove I- and T- since intellisence already marks types and interfaces distinctively
- public class RepairAll: IStrategy
+ public class Repairer: IStrategy

# Took out gag
- class GoodSufTbl
- class GoodSuffixTable

# either this or frostbite/sunburn
- class PF;
+ class PrefixFunction

# This was pre-requisition of the task 
- class MyComplex;
+ class ComplexNumber;


# University patterns project
- public FillDrawer(Color color) {
-     brush = new SolidBrush(color);
- }
+ public SetSolidBrushColor(Color color) {
+     brush = new SolidBrush(color);
+ }


# instance.Hit(point) -> instance.Contains(point) or Decorator.Contains(instance, point)
- public override bool Hit(PointF clickPoint) {
+ public override bool Contains(this Decorator, PointF clickPoint) {

- public override void Move(SizeF size) {
+ public override void Resize(SizeF size) {

- public bool IsAttach => Figure != null;
+ public bool IsAttached => Figure != null;

- def tocnf(self):
+ def to_canonical_normal_form(self):

- public void ProcOrder(Order o)
+ public void ProcessOrder(Order order)

- public void DataLoad()
+ public void LoadData()
```

